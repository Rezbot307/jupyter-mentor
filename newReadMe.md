for running the code in python virtual environment and jupyter labs, use the following commands:

from the jupyter-mentor directory:
`python -m venv venv`
`.\venv\Scripts\Activate`
`pip install jupyterlab nbdev pandas ipywidgets fastcore voila sortedcontainers pypdf`
`pip install langchain-google-genai`
`jupyter lab`

now the code files will open as jupyter notebook in localhost